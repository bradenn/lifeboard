<%
let isActive = (target) => {
    return (url === target || (url.includes('post') && target === '/discussion'))?'active':'';
};
%>
<div class="sidebar">
    <span class="title"><img src="/images/logo.png">Life<b>Board</b></span>
    <a href="#" class="active">Dashboard</a>
    <a href="#">Planner</a>
    <a href="#">Projects</a>
    <a href="#">Notes</a>
    <a href="#">Lists</a>
</div>